# python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# secrets : NEVER commit
.env
.env.local
.env.*.local

# local database (regenerated by seed.py / on startup)
backend/data/*.db

# tooling
.pytest_cache/
.ruff_cache/
.mypy_cache/
.DS_Store

# frontend
frontend/node_modules/
frontend/dist/
frontend/*.code-workspace
.claude/

# matching library cache + outputs
.cache/
data/matches/

# Triage batch scaffolding (local/Fly one-off; CSV holds applicant PII)
batch_input.csv
Dockerfile.batch
Dockerfile.batch.dockerignore
fly.batch.toml
/tmp_results/

# Build stamp baked into the Docker image at build time (see Dockerfile); never
# committed — only generated inside the image so /api/health can report it.
backend/.build_time
